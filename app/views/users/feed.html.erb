<%= render 'bio_section' %>

<section class="section">
  <div class="container">
    <%= render 'tabs' %>
    
  <div data-vue-component="content-feed/base"
       data-base-url="<%= user_url @user %>">
  </div>
  </div>
</section>

